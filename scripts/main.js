require(
    //imports required js files
    [
        'vars/camera',
        'vars/dragon',
        'vars/game',
        'vars/keyBoardInput',
        'vars/mouseInput',
        'vars/platform',
        'vars/projectile',
        'vars/utils'
    ],

    //runs once files are imported
    function () {
        game.setup();
    }
);
